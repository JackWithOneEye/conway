{
  "scripts": {
    "build:watch": "bunx esbuild frontend/index.js frontend/worker.js --bundle --outdir=static --loader:.wasm=binary --watch",
    "tailwind:watch": "bunx tailwindcss -i input.css -o static/output.css --watch"
  },
  "dependencies": {
    "htmx.org": "^1.9.5",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "esbuild": "^0.19.2",
    "tailwindcss": "^3.3.3"
  }
}